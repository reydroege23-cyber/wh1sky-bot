# Runtime dependencies for Railway/Docker production.
# The bot uses python-telegram-bot, not Pyrogram. Keep Pyrogram/TgCrypto out of
# production to avoid native extension builds on Railway.

python-telegram-bot>=21.11,<22
openai>=2.34,<3
python-dotenv>=1.0,<2
